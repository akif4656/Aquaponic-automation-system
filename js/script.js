// script.js

// Example interactivity (you can expand as needed)
document.getElementById('lightToggle').addEventListener('change', function () {
  alert(this.checked ? "Lights turned ON" : "Lights turned OFF");
});

document.getElementById('autoToggle').addEventListener('change', function () {
  alert(this.checked ? "Automation enabled" : "Automation disabled");
});
